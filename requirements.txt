# MoLiM dependencies
#
# As of MoLiM-dyy the cinemagoer dependency is gone - the IMDb data
# layer is now an OMDb + TMDb hybrid (MoLiM-02x). Any modern Python
# (3.11+) works; CI is on 3.11 and 3.12.

# OMDb + TMDb hybrid data layer
requests>=2.32,<3
python-dotenv>=1.0,<2

# Tests
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
